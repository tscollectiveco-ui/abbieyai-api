name = "abbieyai-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
# The worker will handle API routes while static assets are served from /public
pages_build_output_dir = "public"

[ai]
binding = "AI"

# Optional: Configure routes for the worker
# [env.production]
# routes = [
#   { pattern = "example.com/api/*", zone_name = "example.com" }
# ]
